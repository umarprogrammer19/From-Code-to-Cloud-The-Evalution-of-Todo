# Backend Environment Variables
# Copy this file to .env for local development
# For production deployment, set these as environment variables

# JWT Secret - Required for production (will cause error if not set)
# Generate a strong secret using: python -c "import secrets; print(secrets.token_urlsafe(32))"
BETTER_AUTH_SECRET=sk-af8e3b7d2f9c4a1e8b5f6c7a9d2e4f8b1c6d3a7e9f2c4b8d7a1f9e6c5b3a8d2f

# Database URL (optional, defaults to local SQLite if not provided)
# DATABASE_URL=postgresql://user:password@localhost/dbname

# MCP Server Configuration (optional)
MCP_SERVER_URL=http://localhost:8080
MCP_TIMEOUT=30
MCP_MAX_RETRIES=3